import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Mediconnect | Nigerian AI EMR System",
  description: "A lightweight, high-speed Electronic Medical Record (EMR) system designed for the unique realities of Nigerian healthcare. Featuring AI-assisted triage to support clinical decision-making, a live patient queue for seamless hospital flow, and a simplified interface that requires zero technical training for nurses and doctors.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
export const metaData: Metadata = {
  title: "MediConnect | AI EMR",
  description: "Intuitive healthcare management for Nigerian hospitals.",
  // THIS IS THE PART YOU ADD:
  icons: {
    icon: [
      { url: '/favicon-32x32.png', sizes: '32x32', type: 'image/png' },
      { url: '/favicon-16x16.png', sizes: '16x16', type: 'image/png' },
    ],
    apple: [
      { url: '/apple-touch-icon.png' },
    ],
  },
};